# Stub Dockerfile — satisfies DaaS platform build detection.
# This image is never deployed; Azure Cache for Redis is used instead
# (cartDatabase.inClusterRedis.create: false in helm-chart/values.yaml).
FROM redis:alpine
